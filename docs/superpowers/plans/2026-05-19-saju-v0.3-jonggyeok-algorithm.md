# Saju v0.3 종격·격국-용신·strength 통합 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 종격(從格) 판정 + 격국-용신 분기 + lifetime/yearly strength 단일 알고리즘 도입으로 김석곤 2026 평가가 신한라이프와 일치 (favorable) 하도록 만든다.

**Architecture:** B 안 (공유 모듈 추출) — `lib/shen-strength.ts` + `lib/jonggyeok.ts` + `lib/gyeokguk-yongshin.ts` 신규. ko/cn-ziping 두 학파 builder 가 공통 사용. `elements.ts:computeStrength` 도 같은 자평 룰로 교체. ALGORITHM_VERSION 키로 캐시 자동 무효화.

**Tech Stack:** TypeScript (strict) · Vitest · Drizzle ORM · PostgreSQL · pnpm workspaces (`packages/saju` + `apps/dashboard`)

**Spec:** `docs/superpowers/specs/2026-05-19-saju-v0.3-jonggyeok-algorithm-design.md`

---

## File Structure

### 신규 (Create)
- `packages/saju/src/lib/algorithm-version.ts` — `ALGORITHM_VERSION = 2` 상수 + JSDoc
- `packages/saju/src/lib/shen-strength.ts` — 자평 룰 신강도 + 종격 판정 통합
- `packages/saju/src/lib/shen-strength.test.ts` — 10 케이스 트리거 매트릭스
- `packages/saju/src/lib/jonggyeok.ts` — 종격 → 용신 매핑
- `packages/saju/src/lib/jonggyeok.test.ts` — 5 케이스
- `packages/saju/src/lib/gyeokguk-yongshin.ts` — 4 격국 × 2 신강도 분기
- `packages/saju/src/lib/gyeokguk-yongshin.test.ts` — 9 케이스
- `packages/saju/src/lib/branchHiddenStems.ts` — 지장간 상수 표 (12 지지)
- `packages/saju/tests/fixtures/fixture-jonggyeok-complete-jongjae.json` — 완전 종재 합성 명조
- `packages/saju/tests/fixtures/fixture-gyeokguk-shenkang-sangwan.json` — 신강 상관격 → 傷官生財
- `packages/saju/tests/fixtures/fixture-gyeokguk-shenyak-jeonggwan.json` — 신약 정관격 → 官印相生
- `apps/dashboard/drizzle/0014_<random>_*.sql` — pnpm db:generate 산출물

### 수정 (Modify)
- `packages/saju/src/types.ts:38` — Strength type 영어 5-tier → 한국어 6-tier (종격 포함)
- `packages/saju/src/types/yongshin.ts` — KoYongshin/CnZipingYongshin 에 `rationale?: string` 추가, basisShenStrength enum 확장
- `packages/saju/src/elements.ts:28-37` — computeStrength 를 자평 룰 (shen-strength 재사용) 로 교체
- `packages/saju/src/elements.test.ts` — strength 케이스 expected 값 갱신
- `packages/saju/src/pattern.ts` — strength 의존 부분 점검·갱신 (필요 시)
- `packages/saju/src/pattern.test.ts` — 회귀 갱신
- `packages/saju/src/adapters/ko/yongshin.ts` — 3단계 우선순위 (종격 → 격국 → fallback) 재작성
- `packages/saju/src/adapters/ko/yongshin.test.ts` — 김석곤 종아 케이스 + 격국 분기 케이스 추가
- `packages/saju/src/adapters/cn-ziping/yongshin.ts` — 동일 패턴
- `packages/saju/src/adapters/cn-ziping/yongshin.test.ts` — 동일 패턴
- `packages/saju/src/adapters/ko/lifetime.test.ts` — strength 값 회귀 갱신 (필요 시)
- `packages/saju/src/adapters/cn-ziping/lifetime.test.ts` — 동일 (필요 시)
- `packages/saju/src/adapters/cn-mangpai/lifetime.test.ts` — 동일 (필요 시)
- `packages/saju/src/adapters/jp/lifetime.test.ts` — 동일 (필요 시)
- `packages/saju/tests/fixtures/canonical-1967.json` — expected.ko.yongshin + expected.cnZiping.yongshin 추가
- `packages/saju/tests/canonical.test.ts` — 4 fixture loop 갱신
- `apps/dashboard/src/shared/lib/db/schema.ts` — 4 테이블 `algorithm_version` 컬럼 + unique index 갱신
- `apps/dashboard/src/features/saju-lifetime-tri/api/narrative-server.ts` — ALGORITHM_VERSION 캐시 키 추가
- `apps/dashboard/src/features/saju-yearly-tri/api/narrative-server.ts` — 동일
- `apps/dashboard/src/features/saju-lifetime-tri/api/lifetime-server.ts` — algorithm_version 적재
- `apps/dashboard/src/features/saju-yearly-tri/api/yearly-server.ts` — 동일
- `packages/saju/src/computeSajuChart.ts` — strength 계산 호출부

---

## Phase 1: ALGORITHM_VERSION + DB schema

### Task 1.1: ALGORITHM_VERSION 상수 모듈

**Files:**
- Create: `packages/saju/src/lib/algorithm-version.ts`

- [ ] **Step 1: 파일 작성**

```typescript
/**
 * 사주 알고리즘 전체 버전.
 *
 * - v1: v0.2 종료 시점 (단순 개수 룰 + 종격 무, 격국-용신 미연동)
 * - v2: v0.3 도입 (자평 룰 통합 + 종격 + 격국-용신 연동, ko/cn-ziping)
 *
 * bump 트리거: shen-strength / jonggyeok / gyeokguk-yongshin 모듈 변경 시.
 * 캐시 키 (saju_charts, yearly_tri, lifetime_narrative, yearly_narrative) 에 모두 포함되어 자동 무효화.
 */
export const ALGORITHM_VERSION = 2;
```

- [ ] **Step 2: 타입 체크**

Run: `cd packages/saju && pnpm typecheck`
Expected: 0 errors.

- [ ] **Step 3: 커밋**

```bash
git add packages/saju/src/lib/algorithm-version.ts
git commit -m "feat(saju-tri): ALGORITHM_VERSION=2 상수 모듈

v0.2 PROMPT_VERSION 패턴 그대로 — 캐시 키에 포함되어 자동 무효화.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

### Task 1.2: Strength type 확장 (영어 5-tier → 한국어 6-tier)

**Files:**
- Modify: `packages/saju/src/types.ts:38`

- [ ] **Step 1: types.ts 의 Strength 정의 교체**

`packages/saju/src/types.ts` 의 line 38 을 다음으로 교체:

```typescript
// v0.2: "very-strong" | "strong" | "balanced" | "weak" | "very-weak";
// v0.3: 자평 룰 통합 + 종격 도입 — 한국어 6-tier
export type Strength =
  | "신강" | "신약" | "균형"
  | "종아" | "종재" | "종살";
```

- [ ] **Step 2: 타입 체크 — 예상 에러 매핑**

Run: `cd packages/saju && pnpm typecheck 2>&1 | tail -30`
Expected: 다운스트림 에러 다수 발생. 다음 파일들에서 영어 리터럴 strength 사용:
- `elements.ts:32-36` (`if (count >= 4) return "very-strong"`)
- `pattern.ts` (strength 비교)
- 기타 strength 리터럴 사용처

이건 모두 다음 task 들이 수정함. 이 task 에서는 타입만 바꾸고 다른 파일은 건드리지 않음. 다음 task 들이 fix 할 예정이라 commit 만 하고 진행.

- [ ] **Step 3: 커밋**

```bash
git add packages/saju/src/types.ts
git commit -m "feat(saju-tri): Strength type 영어 5-tier → 한국어 6-tier

v0.3 종격(종아·종재·종살) 도입. 다운스트림 에러는 Phase 2~5 에서 해결.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

### Task 1.3: 지장간 (Branch Hidden Stems) 상수 표

**Files:**
- Create: `packages/saju/src/lib/branchHiddenStems.ts`

- [ ] **Step 1: 파일 작성**

```typescript
import type { Branch, Stem } from "../hanja";

/**
 * 지지(地支)의 지장간(地藏干) 표 — 정통 자평 명리.
 *
 * 각 지지에 숨어있는 천간 (주기·중기·여기 순서). 본 표는 v0.3 종격 트리거에서만
 * 사용. 일반 element balance / lifetime balance 등 다른 사용처는 지지의 주기
 * (BRANCH_ELEMENT) 만 사용.
 */
export const BRANCH_HIDDEN_STEMS: Record<Branch, Stem[]> = {
  子: ["癸"],
  丑: ["己", "癸", "辛"],
  寅: ["甲", "丙", "戊"],
  卯: ["乙"],
  辰: ["戊", "乙", "癸"],
  巳: ["丙", "戊", "庚"],
  午: ["丁", "己"],
  未: ["己", "丁", "乙"],
  申: ["庚", "壬", "戊"],
  酉: ["辛"],
  戌: ["戊", "辛", "丁"],
  亥: ["壬", "甲"],
};
```

- [ ] **Step 2: 타입 체크**

Run: `cd packages/saju && pnpm typecheck 2>&1 | tail -20`
Expected: 다운스트림 에러는 그대로 (Strength 미해결). 새 파일 자체는 0 에러.

- [ ] **Step 3: 커밋**

```bash
git add packages/saju/src/lib/branchHiddenStems.ts
git commit -m "feat(saju-tri): 지장간(地藏干) 표 — 12 지지

v0.3 종격 트리거 전용. 다른 사용처는 주기(BRANCH_ELEMENT)만.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

### Task 1.4: DB schema 4 테이블에 algorithm_version 컬럼

**Files:**
- Modify: `apps/dashboard/src/shared/lib/db/schema.ts`

- [ ] **Step 1: 4 테이블 정의 갱신**

각 테이블의 컬럼 블록 + index 정의에 다음 변경:

`saju_charts` 테이블:
```typescript
{
  // ... 기존 컬럼들
  algorithmVersion: integer("algorithm_version").notNull().default(1),
}
```

unique index 갱신 — 기존 unique 가 `(profile_id, input_hash)` 였다면:
```typescript
uniqueIndex("saju_charts_unique_idx").on(
  t.profileId,
  t.inputHash,
  t.algorithmVersion,
)
```

(기존 unique index 이름이 다르면 그대로 유지)

`saju_yearly_tri`:
```typescript
{
  // ... 기존 컬럼 (schema_version 등)
  algorithmVersion: integer("algorithm_version").notNull().default(1),
}

uniqueIndex("saju_yearly_tri_cache_key").on(
  t.profileId,
  t.school,
  t.targetYear,
  t.inputHash,
  t.schemaVersion,
  t.algorithmVersion,
)
```

`saju_lifetime_narrative` (v0.2 의 promptVersion 위에):
```typescript
{
  // ... v0.2 columns including promptVersion
  algorithmVersion: integer("algorithm_version").notNull().default(1),
}

uniqueIndex("saju_lifetime_narrative_cache_key").on(
  t.profileId,
  t.school,
  t.frameHash,
  t.modelId,
  t.promptVersion,
  t.algorithmVersion,
)
```

`saju_yearly_narrative` 동일 패턴.

- [ ] **Step 2: 타입 체크**

Run: `cd apps/dashboard && pnpm typecheck 2>&1 | tail -20`
Expected: Strength 관련 에러는 변함없이 남음. schema.ts 변경에 직접 의존하는 새 에러는 없어야 함.

- [ ] **Step 3: 커밋**

```bash
git add apps/dashboard/src/shared/lib/db/schema.ts
git commit -m "feat(saju-tri): 4 테이블에 algorithm_version 컬럼 + index

saju_charts / yearly_tri / lifetime_narrative / yearly_narrative.
unique index 에 algorithm_version 포함 — v1/v2 캐시 공존.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

### Task 1.5: Drizzle 마이그레이션 생성 + 검토

**Files:**
- Create: `apps/dashboard/drizzle/0014_<random>_*.sql`
- Create: `apps/dashboard/drizzle/meta/0014_snapshot.json`

- [ ] **Step 1: 마이그레이션 생성**

Run: `cd apps/dashboard && pnpm db:generate`
Expected: `drizzle/0014_<random_name>.sql` 파일 생성.

- [ ] **Step 2: 생성된 SQL 검토**

Run: `cat apps/dashboard/drizzle/0014_*.sql`
Expected:

```sql
ALTER TABLE "saju_charts"
  ADD COLUMN "algorithm_version" integer DEFAULT 1 NOT NULL;
ALTER TABLE "saju_yearly_tri"
  ADD COLUMN "algorithm_version" integer DEFAULT 1 NOT NULL;
ALTER TABLE "saju_lifetime_narrative"
  ADD COLUMN "algorithm_version" integer DEFAULT 1 NOT NULL;
ALTER TABLE "saju_yearly_narrative"
  ADD COLUMN "algorithm_version" integer DEFAULT 1 NOT NULL;

DROP INDEX IF EXISTS "saju_charts_unique_idx";
CREATE UNIQUE INDEX "saju_charts_unique_idx"
  ON "saju_charts" USING btree (...);
-- 4 unique index 모두 재생성
```

**컬럼 추가가 unique index 재생성보다 먼저 와야** 함. 순서 뒤바뀌면 수동 편집.

- [ ] **Step 3: 로컬 테스트 DB 적용**

테스트 DB 없으면:
```bash
docker ps | grep gons-test-db || docker run -d --rm --name gons-test-db -p 5999:5432 \
  -e POSTGRES_USER=test -e POSTGRES_PASSWORD=test -e POSTGRES_DB=test_dummy \
  postgres:16-alpine
```

마이그레이션:
```bash
DATABASE_URL="postgres://test:test@127.0.0.1:5999/test_dummy" \
  pnpm --filter @gons/dashboard db:migrate
```

Expected: 0001~0014 모두 적용. 0 errors.

- [ ] **Step 4: 컬럼 존재 확인**

```bash
docker exec gons-test-db psql -U test -d test_dummy -c "\d saju_charts" 2>&1 | grep algorithm_version
docker exec gons-test-db psql -U test -d test_dummy -c "\d saju_yearly_tri" 2>&1 | grep algorithm_version
docker exec gons-test-db psql -U test -d test_dummy -c "\d saju_lifetime_narrative" 2>&1 | grep algorithm_version
docker exec gons-test-db psql -U test -d test_dummy -c "\d saju_yearly_narrative" 2>&1 | grep algorithm_version
```
Expected: 4 테이블 모두 algorithm_version 출력.

- [ ] **Step 5: 커밋**

```bash
git add apps/dashboard/drizzle/0014_*.sql apps/dashboard/drizzle/meta/0014_snapshot.json apps/dashboard/drizzle/meta/_journal.json
git commit -m "feat(saju-tri): v0.3 migration — algorithm_version 컬럼 4 테이블

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Phase 2: shen-strength.ts (TDD)

### Task 2.1: shen-strength.test.ts 작성 (테스트 먼저, RED)

**Files:**
- Create: `packages/saju/src/lib/shen-strength.test.ts`

- [ ] **Step 1: 테스트 파일 작성**

```typescript
import { describe, expect, it } from "vitest";
import { computeShenStrength } from "./shen-strength";
import type { SajuChart } from "../types";

// 합성 명조 헬퍼 — pillars 만 신경 쓰는 case.
function chartOf(
  yearStem: string, yearBranch: string,
  monthStem: string, monthBranch: string,
  dayStem: string, dayBranch: string,
  hourStem: string | null, hourBranch: string | null,
): SajuChart {
  return {
    pillars: {
      year:  { stem: yearStem as any, branch: yearBranch as any },
      month: { stem: monthStem as any, branch: monthBranch as any },
      day:   { stem: dayStem as any, branch: dayBranch as any },
      hour:  hourStem && hourBranch
        ? { stem: hourStem as any, branch: hourBranch as any }
        : null,
    },
    elements: { wood: 0, fire: 0, earth: 0, metal: 0, water: 0 },
    strength: "신약",
    tenGods: {} as any,
    pattern: "",
    yongSin: [],
    giSin: [],
    majorFortunes: [],
    inputHash: "test",
  };
}

describe("computeShenStrength — 김석곤 가종 종아", () => {
  it("1967-03-29 명조 (丁未·癸卯·壬辰·癸卯) → 가종 종아", () => {
    const chart = chartOf("丁","未", "癸","卯", "壬","辰", "癸","卯");
    const result = computeShenStrength(chart);
    expect(result.verdict).toBe("종아");
    expect(result.jonggyeokKind).toBe("가종");
    expect(result.jonggyeokRole).toBe("식상");
  });
});

describe("computeShenStrength — 완전종 케이스", () => {
  it("완전 종아 (인성=0, 비겁=0, 식상 압도)", () => {
    // 일간 甲木, 식상은 火. 戊午·丙午·甲午·丙午 — 일간 제외:
    // 戊(재) 午(식) 午(식) 丙(식) 午(식) 丙(식) 午(식) = 식상 6
    const chart = chartOf("戊","午", "丙","午", "甲","午", "丙","午");
    const result = computeShenStrength(chart);
    expect(result.verdict).toBe("종아");
    expect(result.jonggyeokKind).toBe("완전종");
  });

  it("완전 종재 (인성=0, 비겁≤1, 재성 압도)", () => {
    // 일간 甲木, 재성=土. 戊辰·戊辰·甲辰·戊辰 — 재성 7
    const chart = chartOf("戊","辰", "戊","辰", "甲","辰", "戊","辰");
    const result = computeShenStrength(chart);
    expect(result.verdict).toBe("종재");
    expect(result.jonggyeokKind).toBe("완전종");
  });

  it("완전 종살 (인성=0, 비겁=0, 관성 압도)", () => {
    // 일간 甲木, 관성=金. 庚申·庚申·甲申·庚申 — 관성 7
    const chart = chartOf("庚","申", "庚","申", "甲","申", "庚","申");
    const result = computeShenStrength(chart);
    expect(result.verdict).toBe("종살");
    expect(result.jonggyeokKind).toBe("완전종");
  });
});

describe("computeShenStrength — 일반 신강/신약/균형", () => {
  it("일반 신강 (비겁+인성 우세)", () => {
    // 일간 甲木. 壬子·甲寅·甲寅·壬子 — 인성 4 + 비겁 3 = 7, drain 0
    const chart = chartOf("壬","子", "甲","寅", "甲","寅", "壬","子");
    const result = computeShenStrength(chart);
    expect(result.verdict).toBe("신강");
    expect(result.jonggyeokKind).toBeNull();
  });

  it("일반 신약 (drain 우세, 종격 트리거 미달)", () => {
    // 일간 甲木. drain 골고루, dominant 가 dayHelper*1.3 미달
    const chart = chartOf("甲","午", "戊","辰", "甲","申", "庚","戌");
    const result = computeShenStrength(chart);
    expect(result.verdict).toBe("신약");
    expect(result.jonggyeokKind).toBeNull();
  });

  it("균형 (support ≈ drain)", () => {
    const chart = chartOf("壬","寅", "甲","辰", "甲","午", "戊","申");
    const result = computeShenStrength(chart);
    expect(result.verdict).toBe("균형");
    expect(result.jonggyeokKind).toBeNull();
  });
});

describe("computeShenStrength — 가종 경계 + 트리거 fail", () => {
  it("가종 경계 (인성=1, dominant=4) → 가종 통과", () => {
    const chart = chartOf("庚","卯", "甲","卯", "壬","寅", "乙","卯");
    const result = computeShenStrength(chart);
    expect(result.jonggyeokKind).toBe("가종");
    expect(result.verdict).toBe("종아");
  });

  it("트리거 fail — 인성 2개 (≤1 위반)", () => {
    // 일간 甲木, 인성=水 2개
    const chart = chartOf("壬","子", "甲","午", "甲","午", "丙","午");
    const result = computeShenStrength(chart);
    expect(result.jonggyeokKind).toBeNull();
  });

  it("트리거 fail — dominant < dayHelper × 1.3", () => {
    // 일간 甲木, dayHelper 큼, dominant 모자람
    const chart = chartOf("壬","寅", "甲","寅", "甲","午", "丙","午");
    const result = computeShenStrength(chart);
    expect(result.jonggyeokKind).toBeNull();
  });
});
```

- [ ] **Step 2: 테스트 실행해 fail 확인 (RED)**

Run: `cd packages/saju && TEST_DATABASE_URL="postgres://test:test@127.0.0.1:5999/test_dummy" pnpm test --run src/lib/shen-strength.test.ts`
Expected: `Error: Cannot find module './shen-strength'`.

- [ ] **Step 3: 커밋**

```bash
git add packages/saju/src/lib/shen-strength.test.ts
git commit -m "test(saju-tri): shen-strength 10 케이스 (RED — 구현 전)

종격 트리거 매트릭스: 김석곤 가종 + 완전종 3종 + 신강/신약/균형 + 경계.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

### Task 2.2: shen-strength.ts 구현 (GREEN)

**Files:**
- Create: `packages/saju/src/lib/shen-strength.ts`

- [ ] **Step 1: 모듈 작성**

```typescript
import type { SajuChart } from "../types";
import type { Element, Stem, Branch } from "../hanja";
import { STEM_ELEMENT, BRANCH_ELEMENT } from "../hanja";
import { BRANCH_HIDDEN_STEMS } from "./branchHiddenStems";

export type Role = "비겁" | "인성" | "식상" | "재성" | "관성";

export type ShenVerdict =
  | "신강" | "신약" | "균형"
  | "종아" | "종재" | "종살";

export interface ShenStrengthResult {
  dayElement: Element;
  supportScore: number;
  drainScore: number;
  roleCount: Record<Role, number>;
  roleCountExtended: Record<Role, number>;
  verdict: ShenVerdict;
  jonggyeokKind: "완전종" | "가종" | null;
  jonggyeokRole: Role | null;
}

const PRODUCES: Record<Element, Element> = {
  wood: "fire", fire: "earth", earth: "metal", metal: "water", water: "wood",
};
const PRODUCED_BY: Record<Element, Element> = {
  fire: "wood", earth: "fire", metal: "earth", water: "metal", wood: "water",
};
const CONTROLS: Record<Element, Element> = {
  wood: "earth", fire: "metal", earth: "water", metal: "wood", water: "fire",
};

export function classifyRole(dayEl: Element, target: Element): Role {
  if (target === dayEl) return "비겁";
  if (target === PRODUCED_BY[dayEl]) return "인성";
  if (target === PRODUCES[dayEl]) return "식상";
  if (target === CONTROLS[dayEl]) return "재성";
  return "관성";
}

function emptyRoleCount(): Record<Role, number> {
  return { 비겁: 0, 인성: 0, 식상: 0, 재성: 0, 관성: 0 };
}

function detectJonggyeok(
  roleCount: Record<Role, number>,
): { kind: "완전종" | "가종"; role: Role } | null {
  const { 비겁, 인성, 식상, 재성, 관성 } = roleCount;
  const dayHelperScore = 비겁 + 인성;

  const candidates: { role: Role; count: number }[] = [
    { role: "식상", count: 식상 },
    { role: "재성", count: 재성 },
    { role: "관성", count: 관성 },
  ];
  const dominant = candidates.reduce((max, c) => c.count > max.count ? c : max);

  if (인성 === 0 && 비겁 <= 1 && dominant.count >= 6) {
    return { kind: "완전종", role: dominant.role };
  }

  if (
    인성 <= 1 &&
    dominant.count >= dayHelperScore * 1.3 &&
    dominant.count >= 4
  ) {
    return { kind: "가종", role: dominant.role };
  }

  return null;
}

export function computeShenStrength(chart: SajuChart): ShenStrengthResult {
  const { year, month, day, hour } = chart.pillars;
  const dayElement = STEM_ELEMENT[day.stem];

  // 주기만 카운트 (일간 포함, 비겁으로)
  const stems: Stem[] = [year.stem, month.stem, day.stem];
  const branches: Branch[] = [year.branch, month.branch, day.branch];
  if (hour) {
    stems.push(hour.stem);
    branches.push(hour.branch);
  }

  const roleCount = emptyRoleCount();
  for (const s of stems) {
    const el = STEM_ELEMENT[s];
    roleCount[classifyRole(dayElement, el)]++;
  }
  for (const b of branches) {
    const el = BRANCH_ELEMENT[b];
    roleCount[classifyRole(dayElement, el)]++;
  }

  const supportScore = roleCount["비겁"] + roleCount["인성"];
  const drainScore = roleCount["식상"] + roleCount["재성"] + roleCount["관성"];

  // 지장간 포함 확장 카운트 (종격 트리거 전용)
  const roleCountExtended: Record<Role, number> = { ...roleCount };
  for (const b of branches) {
    const hidden = BRANCH_HIDDEN_STEMS[b];
    for (const hs of hidden) {
      const el = STEM_ELEMENT[hs];
      roleCountExtended[classifyRole(dayElement, el)]++;
    }
  }

  // 종격 판정 (확장 카운트로)
  const jong = detectJonggyeok(roleCountExtended);
  if (jong) {
    const verdict: ShenVerdict =
      jong.role === "식상" ? "종아" :
      jong.role === "재성" ? "종재" : "종살";
    return {
      dayElement,
      supportScore, drainScore,
      roleCount, roleCountExtended,
      verdict,
      jonggyeokKind: jong.kind,
      jonggyeokRole: jong.role,
    };
  }

  // 자평 룰
  const diff = supportScore - drainScore;
  const verdict: ShenVerdict =
    diff >= 2 ? "신강" : diff <= -2 ? "신약" : "균형";

  return {
    dayElement,
    supportScore, drainScore,
    roleCount, roleCountExtended,
    verdict,
    jonggyeokKind: null,
    jonggyeokRole: null,
  };
}
```

- [ ] **Step 2: 테스트 실행 (GREEN)**

Run: `cd packages/saju && TEST_DATABASE_URL="postgres://test:test@127.0.0.1:5999/test_dummy" pnpm test --run src/lib/shen-strength.test.ts`
Expected: 10 케이스 모두 PASS.

만약 일부 fail:
- 합성 명조의 지장간 카운트가 예상과 다를 수 있음 — 실패 case 의 `result.roleCountExtended` 를 console.log 로 찍어서 수치 확인 후 case 의 expected 또는 테스트 chart 조정
- 트리거 조건 (1.3 magic number) 은 spec 명시값이라 변경 금지

- [ ] **Step 3: 타입 체크**

Run: `cd packages/saju && pnpm typecheck 2>&1 | tail -10`
Expected: shen-strength 모듈 자체는 0 에러. 다른 파일의 Strength 에러는 그대로.

- [ ] **Step 4: 커밋**

```bash
git add packages/saju/src/lib/shen-strength.ts
git commit -m "feat(saju-tri): shen-strength 모듈 — 자평 룰 + 종격 트리거

10 케이스 PASS. 지장간 포함 확장 카운트로 종격 판정,
주기만 카운트로 신강/신약/균형 판정.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Phase 3: jonggyeok + gyeokguk-yongshin

### Task 3.1: jonggyeok.test.ts (RED)

**Files:**
- Create: `packages/saju/src/lib/jonggyeok.test.ts`

- [ ] **Step 1: 테스트 작성**

```typescript
import { describe, expect, it } from "vitest";
import { buildJonggyeokYongshin } from "./jonggyeok";
import type { ShenStrengthResult, Role } from "./shen-strength";
import type { Element } from "../hanja";

function shenOf(
  dayEl: Element,
  verdict: ShenStrengthResult["verdict"],
  jonggyeokKind: ShenStrengthResult["jonggyeokKind"],
  jonggyeokRole: Role | null,
  roleCount: Partial<Record<Role, number>> = {},
): ShenStrengthResult {
  const base = { 비겁: 0, 인성: 0, 식상: 0, 재성: 0, 관성: 0, ...roleCount };
  return {
    dayElement: dayEl,
    supportScore: 0,
    drainScore: 0,
    roleCount: base,
    roleCountExtended: base,
    verdict, jonggyeokKind, jonggyeokRole,
  };
}

describe("buildJonggyeokYongshin", () => {
  it("종아 → primary=식상 오행, gisin=[일간, 인성]", () => {
    // 일간 水 (壬), 종아 → 식상은 木. gisin=[water, metal]
    const shen = shenOf("water", "종아", "가종", "식상", { 식상: 4 });
    const result = buildJonggyeokYongshin(shen);
    expect(result).not.toBeNull();
    expect(result!.primary).toBe("wood");
    expect(result!.gisin).toEqual(expect.arrayContaining(["water", "metal"]));
    expect(result!.rationale).toContain("종아");
  });

  it("종재 → primary=재성 오행", () => {
    // 일간 木 (甲), 종재 → 재성은 土. gisin=[wood, water]
    const shen = shenOf("wood", "종재", "완전종", "재성", { 재성: 7 });
    const result = buildJonggyeokYongshin(shen);
    expect(result!.primary).toBe("earth");
    expect(result!.gisin).toEqual(expect.arrayContaining(["wood", "water"]));
  });

  it("종살 → primary=관성 오행", () => {
    // 일간 木 (甲), 종살 → 관성은 金. gisin=[wood, water]
    const shen = shenOf("wood", "종살", "완전종", "관성", { 관성: 7 });
    const result = buildJonggyeokYongshin(shen);
    expect(result!.primary).toBe("metal");
    expect(result!.gisin).toEqual(expect.arrayContaining(["wood", "water"]));
  });

  it("일반 신약 (jonggyeokKind=null) → null 반환", () => {
    const shen = shenOf("wood", "신약", null, null);
    expect(buildJonggyeokYongshin(shen)).toBeNull();
  });

  it("일반 신강 (jonggyeokKind=null) → null 반환", () => {
    const shen = shenOf("wood", "신강", null, null);
    expect(buildJonggyeokYongshin(shen)).toBeNull();
  });
});
```

- [ ] **Step 2: 테스트 실행해 fail 확인 (RED)**

Run: `cd packages/saju && TEST_DATABASE_URL="postgres://test:test@127.0.0.1:5999/test_dummy" pnpm test --run src/lib/jonggyeok.test.ts`
Expected: Cannot find module './jonggyeok'.

- [ ] **Step 3: 커밋**

```bash
git add packages/saju/src/lib/jonggyeok.test.ts
git commit -m "test(saju-tri): jonggyeok 5 케이스 (RED)

종아/종재/종살 매핑 + null 반환 케이스.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

### Task 3.2: jonggyeok.ts 구현 (GREEN)

**Files:**
- Create: `packages/saju/src/lib/jonggyeok.ts`

- [ ] **Step 1: 모듈 작성**

```typescript
import type { Element } from "../hanja";
import type { ShenStrengthResult, Role } from "./shen-strength";

const PRODUCES: Record<Element, Element> = {
  wood: "fire", fire: "earth", earth: "metal", metal: "water", water: "wood",
};
const PRODUCED_BY: Record<Element, Element> = {
  fire: "wood", earth: "fire", metal: "earth", water: "metal", wood: "water",
};
const CONTROLS: Record<Element, Element> = {
  wood: "earth", fire: "metal", earth: "water", metal: "wood", water: "fire",
};
const CONTROLLED_BY: Record<Element, Element> = {
  wood: "metal", fire: "water", earth: "wood", metal: "fire", water: "earth",
};

function roleToElement(dayEl: Element, role: Role): Element {
  switch (role) {
    case "비겁": return dayEl;
    case "인성": return PRODUCED_BY[dayEl];
    case "식상": return PRODUCES[dayEl];
    case "재성": return CONTROLS[dayEl];
    case "관성": return CONTROLLED_BY[dayEl];
  }
}

export interface JonggyeokYongshin {
  primary: Element;
  gisin: Element[];
  rationale: string;
}

export function buildJonggyeokYongshin(
  shen: ShenStrengthResult,
): JonggyeokYongshin | null {
  if (!shen.jonggyeokKind || !shen.jonggyeokRole) return null;

  const primary = roleToElement(shen.dayElement, shen.jonggyeokRole);
  const insungEl = PRODUCED_BY[shen.dayElement];
  const gisin = [shen.dayElement, insungEl];

  const verdictKor =
    shen.verdict === "종아" ? "종아격" :
    shen.verdict === "종재" ? "종재격" : "종살격";

  const r = shen.roleCountExtended;
  const rationale = `인성 ${r.인성}·비겁 ${r.비겁} 약함, ${shen.jonggyeokRole} ${r[shen.jonggyeokRole]} 우세 — ${shen.jonggyeokKind} ${verdictKor}으로 ${shen.jonggyeokRole}의 흐름을 따른다`;

  return { primary, gisin, rationale };
}
```

- [ ] **Step 2: 테스트 실행 (GREEN)**

Run: `cd packages/saju && TEST_DATABASE_URL="postgres://test:test@127.0.0.1:5999/test_dummy" pnpm test --run src/lib/jonggyeok.test.ts`
Expected: 5 케이스 PASS.

- [ ] **Step 3: 커밋**

```bash
git add packages/saju/src/lib/jonggyeok.ts
git commit -m "feat(saju-tri): jonggyeok 모듈 — 종격 용신 매핑

종아/종재/종살 → 따르는 오행을 primary 로, 일간+인성을 gisin 으로.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

### Task 3.3: gyeokguk-yongshin.test.ts (RED)

**Files:**
- Create: `packages/saju/src/lib/gyeokguk-yongshin.test.ts`

- [ ] **Step 1: 테스트 작성**

```typescript
import { describe, expect, it } from "vitest";
import { buildGyeokgukYongshin } from "./gyeokguk-yongshin";
import type { ShenStrengthResult } from "./shen-strength";
import type { SajuChart } from "../types";

function chartWith(dayStem: string): SajuChart {
  return {
    pillars: {
      year: { stem: "甲" as any, branch: "子" as any },
      month: { stem: "甲" as any, branch: "子" as any },
      day: { stem: dayStem as any, branch: "子" as any },
      hour: null,
    },
    elements: { wood:0, fire:0, earth:0, metal:0, water:0 },
    strength: "신약",
    tenGods: {} as any,
    pattern: "",
    yongSin: [], giSin: [],
    majorFortunes: [],
    inputHash: "test",
  };
}

function shenOf(verdict: "신강" | "신약" | "종아"): ShenStrengthResult {
  return {
    dayElement: "water",
    supportScore: 0, drainScore: 0,
    roleCount: { 비겁:0, 인성:0, 식상:0, 재성:0, 관성:0 },
    roleCountExtended: { 비겁:0, 인성:0, 식상:0, 재성:0, 관성:0 },
    verdict,
    jonggyeokKind: verdict === "종아" ? "가종" : null,
    jonggyeokRole: verdict === "종아" ? "식상" : null,
  };
}

describe("buildGyeokgukYongshin — 상관격", () => {
  it("신강 + 傷官格 → 傷官生財 (재성 용신)", () => {
    const chart = chartWith("壬");
    const result = buildGyeokgukYongshin(chart, shenOf("신강"), "傷官格");
    expect(result!.pattern).toBe("傷官生財");
    expect(result!.primary).toBe("fire");  // 재성 = 水가 극하는 火
  });

  it("신약 + 傷官格 → 傷官佩印 (인성 용신)", () => {
    const chart = chartWith("壬");
    const result = buildGyeokgukYongshin(chart, shenOf("신약"), "傷官格");
    expect(result!.pattern).toBe("傷官佩印");
    expect(result!.primary).toBe("metal");  // 인성 = 水를 생하는 金
  });
});

describe("buildGyeokgukYongshin — 정관격", () => {
  it("신강 + 正官格 → 財官相生 (재성 용신)", () => {
    const chart = chartWith("壬");
    const result = buildGyeokgukYongshin(chart, shenOf("신강"), "正官格");
    expect(result!.pattern).toBe("財官相生");
    expect(result!.primary).toBe("fire");
  });

  it("신약 + 正官格 → 官印相生 (인성 용신)", () => {
    const chart = chartWith("壬");
    const result = buildGyeokgukYongshin(chart, shenOf("신약"), "正官格");
    expect(result!.pattern).toBe("官印相生");
    expect(result!.primary).toBe("metal");
  });
});

describe("buildGyeokgukYongshin — 재성격", () => {
  it("신강 + 財格 → 食傷生財 (식상 용신)", () => {
    const chart = chartWith("壬");
    const result = buildGyeokgukYongshin(chart, shenOf("신강"), "財格");
    expect(result!.pattern).toBe("食傷生財");
    expect(result!.primary).toBe("wood");  // 식상 = 水가 생하는 木
  });

  it("신약 + 財格 → 比劫制財 (비겁 용신)", () => {
    const chart = chartWith("壬");
    const result = buildGyeokgukYongshin(chart, shenOf("신약"), "財格");
    expect(result!.pattern).toBe("比劫制財");
    expect(result!.primary).toBe("water");  // 비겁 = 일간 水
  });
});

describe("buildGyeokgukYongshin — 편인격", () => {
  it("신강 + 偏印格 → 食神制偏印 (식상 용신)", () => {
    const chart = chartWith("壬");
    const result = buildGyeokgukYongshin(chart, shenOf("신강"), "偏印格");
    expect(result!.pattern).toBe("食神制偏印");
    expect(result!.primary).toBe("wood");
  });

  it("신약 + 偏印格 → 印比相生 (비겁 용신)", () => {
    const chart = chartWith("壬");
    const result = buildGyeokgukYongshin(chart, shenOf("신약"), "偏印格");
    expect(result!.pattern).toBe("印比相生");
    expect(result!.primary).toBe("water");
  });
});

describe("buildGyeokgukYongshin — 종격 시 null + 기타 격국 null", () => {
  it("종격이면 null 반환", () => {
    const chart = chartWith("壬");
    const shen = shenOf("종아");
    expect(buildGyeokgukYongshin(chart, shen, "傷官格")).toBeNull();
  });

  it("기타 격국 (식신격 등) → null 반환", () => {
    const chart = chartWith("壬");
    const result = buildGyeokgukYongshin(chart, shenOf("신약"), "食神格");
    expect(result).toBeNull();
  });
});
```

- [ ] **Step 2: 테스트 실행 (RED)**

Run: `cd packages/saju && TEST_DATABASE_URL="postgres://test:test@127.0.0.1:5999/test_dummy" pnpm test --run src/lib/gyeokguk-yongshin.test.ts`
Expected: Cannot find module.

- [ ] **Step 3: 커밋**

```bash
git add packages/saju/src/lib/gyeokguk-yongshin.test.ts
git commit -m "test(saju-tri): gyeokguk-yongshin 9 케이스 (RED)

4 격국 × 2 신강도 = 8 분기 + 종격/기타 null.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

### Task 3.4: gyeokguk-yongshin.ts 구현 (GREEN)

**Files:**
- Create: `packages/saju/src/lib/gyeokguk-yongshin.ts`

- [ ] **Step 1: 모듈 작성**

```typescript
import type { SajuChart } from "../types";
import type { Element } from "../hanja";
import { STEM_ELEMENT } from "../hanja";
import type { ShenStrengthResult } from "./shen-strength";

const PRODUCES: Record<Element, Element> = {
  wood: "fire", fire: "earth", earth: "metal", metal: "water", water: "wood",
};
const PRODUCED_BY: Record<Element, Element> = {
  fire: "wood", earth: "fire", metal: "earth", water: "metal", wood: "water",
};
const CONTROLS: Record<Element, Element> = {
  wood: "earth", fire: "metal", earth: "water", metal: "wood", water: "fire",
};
const CONTROLLED_BY: Record<Element, Element> = {
  wood: "metal", fire: "water", earth: "wood", metal: "fire", water: "earth",
};

export type GyeokgukPattern =
  | "傷官生財" | "傷官佩印"
  | "官印相生" | "財官相生"
  | "比劫制財" | "食傷生財"
  | "食神制偏印" | "印比相生"
  | "기타";

export interface GyeokgukYongshin {
  primary: Element;
  gisin: Element[];
  pattern: GyeokgukPattern;
  rationale: string;
}

const SUPPORTED_GYEOKGUK = new Set(["傷官格", "正官格", "財格", "偏印格"]);

export function buildGyeokgukYongshin(
  chart: SajuChart,
  shen: ShenStrengthResult,
  gyeokguk: string,
): GyeokgukYongshin | null {
  if (shen.jonggyeokKind) return null;
  if (!SUPPORTED_GYEOKGUK.has(gyeokguk)) return null;
  if (shen.verdict !== "신강" && shen.verdict !== "신약") return null;

  const dayEl = STEM_ELEMENT[chart.pillars.day.stem];
  const sangwan = PRODUCES[dayEl];
  const insung = PRODUCED_BY[dayEl];
  const jaeseong = CONTROLS[dayEl];
  const gwanseong = CONTROLLED_BY[dayEl];
  const bigup = dayEl;

  if (gyeokguk === "傷官格") {
    if (shen.verdict === "신강") {
      return {
        primary: jaeseong, gisin: [insung, bigup],
        pattern: "傷官生財",
        rationale: "신강 상관격 — 재성으로 상관을 흘려 보냄",
      };
    }
    return {
      primary: insung, gisin: [sangwan, jaeseong],
      pattern: "傷官佩印",
      rationale: "신약 상관격 — 인성으로 상관을 제어",
    };
  }

  if (gyeokguk === "正官格") {
    if (shen.verdict === "신강") {
      return {
        primary: jaeseong, gisin: [bigup],
        pattern: "財官相生",
        rationale: "신강 정관격 — 재성이 관성을 생함",
      };
    }
    return {
      primary: insung, gisin: [jaeseong, sangwan],
      pattern: "官印相生",
      rationale: "신약 정관격 — 관성이 인성을 생함",
    };
  }

  if (gyeokguk === "財格") {
    if (shen.verdict === "신강") {
      return {
        primary: sangwan, gisin: [insung, bigup],
        pattern: "食傷生財",
        rationale: "신강 재성격 — 식상이 재성을 생함",
      };
    }
    return {
      primary: bigup, gisin: [jaeseong, gwanseong],
      pattern: "比劫制財",
      rationale: "신약 재성격 — 비겁으로 재성을 다스림",
    };
  }

  if (gyeokguk === "偏印格") {
    if (shen.verdict === "신강") {
      return {
        primary: sangwan, gisin: [insung, bigup],
        pattern: "食神制偏印",
        rationale: "신강 편인격 — 식신으로 편인을 제어",
      };
    }
    return {
      primary: bigup, gisin: [jaeseong, sangwan],
      pattern: "印比相生",
      rationale: "신약 편인격 — 인성과 비겁이 상생",
    };
  }

  return null;
}
```

- [ ] **Step 2: 테스트 실행 (GREEN)**

Run: `cd packages/saju && TEST_DATABASE_URL="postgres://test:test@127.0.0.1:5999/test_dummy" pnpm test --run src/lib/gyeokguk-yongshin.test.ts`
Expected: 9 케이스 모두 PASS.

- [ ] **Step 3: 커밋**

```bash
git add packages/saju/src/lib/gyeokguk-yongshin.ts
git commit -m "feat(saju-tri): gyeokguk-yongshin — 4 격국 × 2 신강도 분기

상관격·정관격·재성격·편인격 각각 신강/신약 분기. 종격 우선.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Phase 4: ko / cn-ziping yongshin builder 갱신

### Task 4.1: types/yongshin.ts 확장

**Files:**
- Modify: `packages/saju/src/types/yongshin.ts`

- [ ] **Step 1: 인터페이스 확장**

기존 파일 전체 교체:

```typescript
import type { Stem, Element } from "../hanja";
import type { ShenVerdict } from "../lib/shen-strength";

export interface KoYongshin {
  school: "ko";
  primary: Element;
  secondary?: Element;
  gisin: Element[];
  basisShenStrength: ShenVerdict;     // v0.3: 6값
  basisJohuMode: "한랭" | "조열" | "균형";
  rationale?: string;                 // v0.3 신규
}

export interface CnZipingYongshin {
  school: "cn-ziping";
  primary: Element;
  gisin: Element[];
  basisShenStrength: ShenVerdict;     // v0.3: 6값
  structureHint?: "식신생재" | "관인상생" | "기타";
  rationale?: string;                 // v0.3 신규
}

export interface CnMangpaiYongshin {
  school: "cn-mangpai";
  primary: Element;
  gisin: Element[];
  emergenceHint: string;
}

export interface JpYongshin {
  school: "jp";
  favorable: string[];
  unfavorable: string[];
}

export type Yongshin = KoYongshin | CnZipingYongshin | CnMangpaiYongshin | JpYongshin;

export interface ShenStrengthBasis {
  dayStem: Stem;
  monthBranch: string;
  supportScore: number;
  drainScore: number;
  verdict: ShenVerdict;               // v0.3: 6값
}
```

- [ ] **Step 2: 타입 체크**

Run: `cd packages/saju && pnpm typecheck 2>&1 | tail -20`
Expected: yongshin 사용처에 새 에러는 없을 가능성. 단 verdict 비교 조건 (`=== "신강"`) 은 아직 유효.

- [ ] **Step 3: 커밋**

```bash
git add packages/saju/src/types/yongshin.ts
git commit -m "feat(saju-tri): KoYongshin/CnZipingYongshin 에 rationale + ShenVerdict

v0.3: basisShenStrength 6값 확장 + rationale optional 필드.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

### Task 4.2: ko/yongshin.ts 갱신 — 3단계 우선순위

**Files:**
- Modify: `packages/saju/src/adapters/ko/yongshin.ts`

- [ ] **Step 1: 파일 전체 교체**

```typescript
import type { SajuChart } from "../../types";
import type { Branch, Element } from "../../hanja";
import { STEM_ELEMENT } from "../../hanja";
import type { KoYongshin } from "../../types/yongshin";
import { computeShenStrength } from "../../lib/shen-strength";
import { buildJonggyeokYongshin } from "../../lib/jonggyeok";
import { buildGyeokgukYongshin } from "../../lib/gyeokguk-yongshin";

const PRODUCES: Record<Element, Element> = {
  wood: "fire", fire: "earth", earth: "metal", metal: "water", water: "wood",
};
const PRODUCED_BY: Record<Element, Element> = {
  fire: "wood", earth: "fire", metal: "earth", water: "metal", wood: "water",
};
const CONTROLS: Record<Element, Element> = {
  wood: "earth", fire: "metal", earth: "water", metal: "wood", water: "fire",
};

const BRANCH_SEASON: Record<Branch, "봄" | "여름" | "가을" | "겨울"> = {
  寅: "봄", 卯: "봄", 辰: "봄",
  巳: "여름", 午: "여름", 未: "여름",
  申: "가을", 酉: "가을", 戌: "가을",
  亥: "겨울", 子: "겨울", 丑: "겨울",
};

function computeJohuMode(monthBranch: Branch): "한랭" | "조열" | "균형" {
  const season = BRANCH_SEASON[monthBranch];
  if (season === "겨울") return "한랭";
  if (season === "여름") return "조열";
  return "균형";
}

/**
 * 한국식 자평 + 조후 혼합 — v0.3.
 *
 * 우선순위:
 *  1. 종격 (jonggyeokKind != null)
 *  2. 격국 분기 (4 격국 × 2 신강도)
 *  3. fallback — 기본 자평
 *
 * 조후 보조용신은 항상 결합 — 한랭 → 火 / 조열 → 水.
 */
export function buildYongshinKo(chart: SajuChart): KoYongshin {
  const shen = computeShenStrength(chart);
  const johu = computeJohuMode(chart.pillars.month.branch);
  const dayElement = STEM_ELEMENT[chart.pillars.day.stem];

  let secondary: Element | undefined;
  if (johu === "한랭") secondary = "fire";
  else if (johu === "조열") secondary = "water";

  // 1순위: 종격
  const jong = buildJonggyeokYongshin(shen);
  if (jong) {
    return {
      school: "ko",
      primary: jong.primary,
      secondary,
      gisin: jong.gisin,
      basisShenStrength: shen.verdict,
      basisJohuMode: johu,
      rationale: jong.rationale,
    };
  }

  // 2순위: 격국 분기
  const gyeok = buildGyeokgukYongshin(chart, shen, chart.pattern);
  if (gyeok) {
    return {
      school: "ko",
      primary: gyeok.primary,
      secondary,
      gisin: gyeok.gisin,
      basisShenStrength: shen.verdict,
      basisJohuMode: johu,
      rationale: gyeok.rationale,
    };
  }

  // 3순위: fallback (기존 v0.2 로직)
  let primary: Element;
  let gisin: Element[];
  if (shen.verdict === "신강") {
    primary = PRODUCES[dayElement];
    gisin = [PRODUCED_BY[dayElement], dayElement];
  } else if (shen.verdict === "신약") {
    primary = PRODUCED_BY[dayElement];
    gisin = [PRODUCES[dayElement], CONTROLS[dayElement]];
  } else {
    primary = PRODUCES[dayElement];
    gisin = [];
  }

  // 조후 충돌 시 조후 우선
  if (secondary && gisin.includes(secondary)) {
    [primary, secondary] = [secondary, primary];
    gisin = gisin.filter((g) => g !== primary);
  }

  return {
    school: "ko",
    primary, secondary, gisin,
    basisShenStrength: shen.verdict,
    basisJohuMode: johu,
  };
}
```

- [ ] **Step 2: 타입 체크**

Run: `cd packages/saju && pnpm typecheck 2>&1 | tail -20`
Expected: ko/yongshin.ts 자체 0 에러.

- [ ] **Step 3: 커밋**

```bash
git add packages/saju/src/adapters/ko/yongshin.ts
git commit -m "feat(saju-tri): ko/yongshin v0.3 — 3단계 우선순위

종격 → 격국 → fallback. 조후 보조용신은 항상 결합.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

### Task 4.3: ko/yongshin.test.ts 갱신

**Files:**
- Modify: `packages/saju/src/adapters/ko/yongshin.test.ts`

- [ ] **Step 1: 기존 + 신규 케이스 통합**

```typescript
import { describe, expect, it } from "vitest";
import { buildYongshinKo } from "./yongshin";
import type { SajuChart } from "../../types";

function chartOf(
  yearStem: string, yearBranch: string,
  monthStem: string, monthBranch: string,
  dayStem: string, dayBranch: string,
  hourStem: string | null, hourBranch: string | null,
  pattern = "",
): SajuChart {
  return {
    pillars: {
      year:  { stem: yearStem as any, branch: yearBranch as any },
      month: { stem: monthStem as any, branch: monthBranch as any },
      day:   { stem: dayStem as any, branch: dayBranch as any },
      hour:  hourStem && hourBranch
        ? { stem: hourStem as any, branch: hourBranch as any }
        : null,
    },
    elements: { wood:0, fire:0, earth:0, metal:0, water:0 },
    strength: "신약",
    tenGods: {} as any,
    pattern,
    yongSin: [], giSin: [],
    majorFortunes: [],
    inputHash: "test",
  };
}

describe("buildYongshinKo — v0.3 종격 우선", () => {
  it("김석곤 1967-03-29 (가종 종아) → primary=木, gisin=[水,金]", () => {
    const chart = chartOf("丁","未", "癸","卯", "壬","辰", "癸","卯", "傷官格");
    const result = buildYongshinKo(chart);
    expect(result.basisShenStrength).toBe("종아");
    expect(result.primary).toBe("wood");
    expect(result.gisin).toEqual(expect.arrayContaining(["water", "metal"]));
  });
});

describe("buildYongshinKo — 격국 분기 / fallback / 조후", () => {
  it("신약 상관격 → 傷官佩印 가능성 (종격 트리거 미달 시)", () => {
    const chart = chartOf("壬","申", "乙","卯", "壬","午", "丙","午", "傷官格");
    const result = buildYongshinKo(chart);
    if (result.basisShenStrength === "신약") {
      expect(result.primary).toBe("metal");
    }
  });

  it("신강 정관격 → 財官相生 가능성", () => {
    const chart = chartOf("壬","子", "甲","寅", "甲","寅", "辛","未", "正官格");
    const result = buildYongshinKo(chart);
    if (result.basisShenStrength === "신강") {
      expect(result.primary).toBe("earth");
    }
  });

  it("기타 격국 + 신약 → fallback 인성 용신 (또는 조후 swap)", () => {
    const chart = chartOf("辛","酉", "丙","申", "甲","午", "戊","辰", "");
    const result = buildYongshinKo(chart);
    if (result.basisShenStrength === "신약") {
      expect(["water"]).toContain(result.primary);
    }
  });

  it("겨울 월령 → secondary=火", () => {
    const chart = chartOf("甲","寅", "丙","子", "甲","寅", "甲","寅", "");
    const result = buildYongshinKo(chart);
    expect(result.basisJohuMode).toBe("한랭");
  });

  it("여름 월령 → secondary=水", () => {
    const chart = chartOf("甲","寅", "丙","午", "甲","寅", "甲","寅", "");
    const result = buildYongshinKo(chart);
    expect(result.basisJohuMode).toBe("조열");
  });
});
```

- [ ] **Step 2: 테스트 실행**

Run: `cd packages/saju && TEST_DATABASE_URL="postgres://test:test@127.0.0.1:5999/test_dummy" pnpm test --run src/adapters/ko/yongshin.test.ts`
Expected: 모든 PASS.

- [ ] **Step 3: 커밋**

```bash
git add packages/saju/src/adapters/ko/yongshin.test.ts
git commit -m "test(saju-tri): ko/yongshin v0.3 — 종격·격국·fallback·조후

김석곤 가종 종아 + 격국 분기 + 조후.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

### Task 4.4: cn-ziping/yongshin.ts 갱신

**Files:**
- Modify: `packages/saju/src/adapters/cn-ziping/yongshin.ts`

- [ ] **Step 1: 파일 전체 교체**

```typescript
import type { SajuChart } from "../../types";
import type { Element } from "../../hanja";
import { STEM_ELEMENT } from "../../hanja";
import type { CnZipingYongshin } from "../../types/yongshin";
import { computeShenStrength } from "../../lib/shen-strength";
import { buildJonggyeokYongshin } from "../../lib/jonggyeok";
import { buildGyeokgukYongshin } from "../../lib/gyeokguk-yongshin";

const PRODUCES: Record<Element, Element> = {
  wood: "fire", fire: "earth", earth: "metal", metal: "water", water: "wood",
};
const PRODUCED_BY: Record<Element, Element> = {
  fire: "wood", earth: "fire", metal: "earth", water: "metal", wood: "water",
};
const CONTROLS: Record<Element, Element> = {
  wood: "earth", fire: "metal", earth: "water", metal: "wood", water: "fire",
};

/**
 * 중국 자평 — 억부 단일 룰 (조후 미반영) — v0.3.
 *
 * 우선순위: 종격 → 격국 → fallback.
 */
export function buildYongshinCnZiping(chart: SajuChart): CnZipingYongshin {
  const shen = computeShenStrength(chart);
  const dayElement = STEM_ELEMENT[chart.pillars.day.stem];

  // 1순위: 종격
  const jong = buildJonggyeokYongshin(shen);
  if (jong) {
    return {
      school: "cn-ziping",
      primary: jong.primary,
      gisin: jong.gisin,
      basisShenStrength: shen.verdict,
      structureHint: "기타",
      rationale: jong.rationale,
    };
  }

  // 2순위: 격국 분기
  const gyeok = buildGyeokgukYongshin(chart, shen, chart.pattern);
  if (gyeok) {
    let structureHint: "식신생재" | "관인상생" | "기타" = "기타";
    if (gyeok.pattern === "食傷生財") structureHint = "식신생재";
    else if (gyeok.pattern === "官印相生") structureHint = "관인상생";
    return {
      school: "cn-ziping",
      primary: gyeok.primary,
      gisin: gyeok.gisin,
      basisShenStrength: shen.verdict,
      structureHint,
      rationale: gyeok.rationale,
    };
  }

  // 3순위: fallback
  let primary: Element;
  let gisin: Element[];
  if (shen.verdict === "신강") {
    primary = PRODUCES[dayElement];
    gisin = [PRODUCED_BY[dayElement], dayElement];
  } else if (shen.verdict === "신약") {
    primary = PRODUCED_BY[dayElement];
    gisin = [PRODUCES[dayElement], CONTROLS[dayElement]];
  } else {
    primary = PRODUCES[dayElement];
    gisin = [];
  }

  let structureHint: "식신생재" | "관인상생" | "기타" = "기타";
  if (shen.verdict === "신강" && shen.roleCount["식상"] >= 1 && shen.roleCount["재성"] >= 1) {
    structureHint = "식신생재";
  } else if (shen.verdict === "신약" && shen.roleCount["관성"] >= 1 && shen.roleCount["인성"] >= 1) {
    structureHint = "관인상생";
  }

  return {
    school: "cn-ziping",
    primary, gisin,
    basisShenStrength: shen.verdict,
    structureHint,
  };
}
```

- [ ] **Step 2: 타입 체크**

Run: `cd packages/saju && pnpm typecheck 2>&1 | tail -10`
Expected: 0 에러.

- [ ] **Step 3: 커밋**

```bash
git add packages/saju/src/adapters/cn-ziping/yongshin.ts
git commit -m "feat(saju-tri): cn-ziping/yongshin v0.3 — 3단계 우선순위

종격 → 격국 → fallback. structureHint 식신생재/관인상생 유지.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

### Task 4.5: cn-ziping/yongshin.test.ts 갱신

**Files:**
- Modify: `packages/saju/src/adapters/cn-ziping/yongshin.test.ts`

- [ ] **Step 1: 케이스 갱신**

```typescript
import { describe, expect, it } from "vitest";
import { buildYongshinCnZiping } from "./yongshin";
import type { SajuChart } from "../../types";

function chartOf(
  yearStem: string, yearBranch: string,
  monthStem: string, monthBranch: string,
  dayStem: string, dayBranch: string,
  hourStem: string | null, hourBranch: string | null,
  pattern = "",
): SajuChart {
  return {
    pillars: {
      year:  { stem: yearStem as any, branch: yearBranch as any },
      month: { stem: monthStem as any, branch: monthBranch as any },
      day:   { stem: dayStem as any, branch: dayBranch as any },
      hour:  hourStem && hourBranch
        ? { stem: hourStem as any, branch: hourBranch as any }
        : null,
    },
    elements: { wood:0, fire:0, earth:0, metal:0, water:0 },
    strength: "신약",
    tenGods: {} as any,
    pattern,
    yongSin: [], giSin: [],
    majorFortunes: [],
    inputHash: "test",
  };
}

describe("buildYongshinCnZiping — v0.3 종격 우선", () => {
  it("김석곤 (가종 종아) → primary=木, gisin=[水,金], structureHint=기타", () => {
    const chart = chartOf("丁","未", "癸","卯", "壬","辰", "癸","卯", "傷官格");
    const result = buildYongshinCnZiping(chart);
    expect(result.basisShenStrength).toBe("종아");
    expect(result.primary).toBe("wood");
    expect(result.gisin).toEqual(expect.arrayContaining(["water", "metal"]));
    expect(result.structureHint).toBe("기타");
  });
});

describe("buildYongshinCnZiping — 격국 분기 / fallback", () => {
  it("신강 + 傷官格 → 傷官生財 가능성", () => {
    const chart = chartOf("壬","子", "甲","寅", "壬","子", "丙","午", "傷官格");
    const result = buildYongshinCnZiping(chart);
    if (result.basisShenStrength === "신강") {
      expect(result.primary).toBe("fire");
    }
  });

  it("신약 + 正官格 → 官印相生 가능성", () => {
    const chart = chartOf("辛","酉", "丙","申", "甲","午", "戊","辰", "正官格");
    const result = buildYongshinCnZiping(chart);
    if (result.basisShenStrength === "신약") {
      expect(result.primary).toBe("water");
      expect(result.structureHint).toBe("관인상생");
    }
  });

  it("기타 격국 + 신강 → 식상 용신 (식신생재 hint)", () => {
    const chart = chartOf("壬","子", "甲","寅", "甲","寅", "丙","午", "");
    const result = buildYongshinCnZiping(chart);
    if (result.basisShenStrength === "신강") {
      expect(result.primary).toBe("fire");
    }
  });
});
```

- [ ] **Step 2: 테스트 실행 + 커밋**

```bash
cd packages/saju && TEST_DATABASE_URL="postgres://test:test@127.0.0.1:5999/test_dummy" pnpm test --run src/adapters/cn-ziping/yongshin.test.ts
git add packages/saju/src/adapters/cn-ziping/yongshin.test.ts
git commit -m "test(saju-tri): cn-ziping/yongshin v0.3 케이스

김석곤 가종 + 격국 분기 + fallback.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Phase 5: elements.ts 자평 룰 교체 + 회귀

### Task 5.1: elements.ts:computeStrength 교체

**Files:**
- Modify: `packages/saju/src/elements.ts`

- [ ] **Step 1: 함수 교체**

기존 `computeStrength(elements, dayStem)` 시그너처는 유지하되 한국어 enum 반환:

```typescript
import type { Stem } from "./hanja";
import { STEM_ELEMENT, BRANCH_ELEMENT } from "./hanja";
import type { SajuPillars, ElementCount, Strength } from "./types";

export function computeElements(pillars: SajuPillars): ElementCount {
  // ... 기존 본문 그대로
}

/**
 * v0.3: 자평 룰 호환 시그너처.
 *
 * 이전 v0.2 의 단순 개수 룰 (영어 5-tier) 을 한국어 3-tier (신강/균형/신약) 로 교체.
 * 정확한 자평 + 종격 판정은 SajuChart 전체가 필요 — computeSajuChart 가
 * computeShenStrength 를 직접 호출. 이 함수는 호환 유지용 fallback.
 */
export function computeStrength(elements: ElementCount, dayStem: Stem): Strength {
  const dayEl = STEM_ELEMENT[dayStem];
  const count = elements[dayEl];
  const total = (Object.values(elements) as number[]).reduce((s, v) => s + v, 0);
  const ratio = total > 0 ? count / total : 0;

  if (ratio >= 0.4) return "신강";
  if (ratio >= 0.25) return "균형";
  return "신약";
}
```

- [ ] **Step 2: 타입 체크**

Run: `cd packages/saju && pnpm typecheck 2>&1 | tail -10`
Expected: elements.ts 자체 0 에러. pattern.ts 가 strength 영어 리터럴 비교한다면 다음 task 에서 fix.

- [ ] **Step 3: 커밋**

```bash
git add packages/saju/src/elements.ts
git commit -m "feat(saju-tri): elements.computeStrength 자평 룰 호환 시그너처

영어 5-tier → 한국어 3-tier. 정확한 자평+종격은 computeSajuChart 가
computeShenStrength 직접 호출.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

### Task 5.2: computeSajuChart.ts 의 strength 마이그레이션

**Files:**
- Modify: `packages/saju/src/computeSajuChart.ts`

- [ ] **Step 1: 파일 읽고 strength 호출 위치 식별**

Run: `cd packages/saju && grep -n "computeStrength\|strength" src/computeSajuChart.ts | head -10`

Expected: `import { computeStrength }` + `strength: computeStrength(...)` 라인 식별.

- [ ] **Step 2: computeShenStrength 호출로 교체**

```typescript
// 기존
import { computeStrength } from "./elements";
// 교체
import { computeShenStrength } from "./lib/shen-strength";
```

```typescript
// 기존 호출 (예시)
const strength = computeStrength(elements, pillars.day.stem);

// 교체 — SajuChart 부분 객체를 합성해 shen-strength 호출
const partialChart = {
  pillars,
  elements,
  strength: "신약",  // 임시 — computeShenStrength 가 이 필드 미사용
  tenGods: {} as any,
  pattern: "",
  yongSin: [], giSin: [],
  majorFortunes: [],
  inputHash: "",
} as SajuChart;
const strength = computeShenStrength(partialChart).verdict;
```

- [ ] **Step 3: 타입 체크**

Run: `cd packages/saju && pnpm typecheck 2>&1 | tail -20`
Expected: computeSajuChart 자체 0 에러. 다른 lifetime 빌더가 영어 리터럴 비교한다면 다음 task 에서 fix.

- [ ] **Step 4: 커밋**

```bash
git add packages/saju/src/computeSajuChart.ts
git commit -m "feat(saju-tri): computeSajuChart strength 를 computeShenStrength 호출로

자평 룰 + 종격 판정 단일 알고리즘 진입점.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

### Task 5.3: pattern.ts + 회귀 테스트 갱신

**Files:**
- Modify: `packages/saju/src/pattern.ts` (필요 시)
- Modify: `packages/saju/src/pattern.test.ts`
- Modify: `packages/saju/src/elements.test.ts`

- [ ] **Step 1: 영어 리터럴 사용처 식별**

Run: `cd packages/saju && grep -rn '"very-strong"\|"strong"\|"balanced"\|"weak"\|"very-weak"' src/ 2>&1 | grep -v node_modules | head -20`
Expected: 모든 영어 리터럴 위치 출력.

- [ ] **Step 2: 영어 → 한국어 매핑 일괄 교체**

각 파일에서:
- `"very-strong"`, `"strong"` → `"신강"`
- `"balanced"` → `"균형"`
- `"weak"`, `"very-weak"` → `"신약"`

pattern.ts 의 strength 비교가 있으면 한국어로. pattern.test.ts / elements.test.ts 의 expected 값도 한국어로.

- [ ] **Step 3: 테스트 실행**

Run: `cd packages/saju && TEST_DATABASE_URL="postgres://test:test@127.0.0.1:5999/test_dummy" pnpm test --run src/pattern.test.ts src/elements.test.ts`
Expected: 모든 PASS.

만약 fail 시 expected 값을 새 알고리즘 결과로 조정 (단 알고리즘 의도와 충돌하는지 검토).

- [ ] **Step 4: 커밋**

```bash
git add packages/saju/src/pattern.ts packages/saju/src/pattern.test.ts packages/saju/src/elements.test.ts
git commit -m "refactor(saju-tri): pattern + elements 회귀 — 한국어 strength

영어 5-tier → 한국어 3-tier 일괄 교체.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

### Task 5.4: 4학파 lifetime.test.ts 회귀

**Files:**
- Modify: `packages/saju/src/adapters/{ko,cn-ziping,cn-mangpai,jp}/lifetime.test.ts` (필요 시)

- [ ] **Step 1: 전체 saju 테스트 실행**

Run: `cd packages/saju && TEST_DATABASE_URL="postgres://test:test@127.0.0.1:5999/test_dummy" pnpm test --run 2>&1 | tail -50`
Expected: 다수 PASS. fail 케이스가 있으면 어떤 파일이 strength 값 비교에서 깨졌는지 식별.

- [ ] **Step 2: fail 케이스 갱신**

각 fail 의 영어 strength 를 한국어로 교체. 또는 strength 비교가 v0.3 결과와 의도적으로 다르면 case 자체 갱신.

- [ ] **Step 3: 다시 실행해 모두 PASS 확인**

Run: `cd packages/saju && TEST_DATABASE_URL="postgres://test:test@127.0.0.1:5999/test_dummy" pnpm test --run`
Expected: 0 fail.

- [ ] **Step 4: 커밋**

```bash
git add packages/saju/src/adapters/
git commit -m "test(saju-tri): 4학파 lifetime.test 회귀 — 한국어 strength

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Phase 6: Canonical fixture 확장

### Task 6.1: canonical-1967.json 갱신

**Files:**
- Modify: `packages/saju/tests/fixtures/canonical-1967.json`

- [ ] **Step 1: expected 에 yongshin 추가**

기존:
```json
{
  "input": { ... },
  "expected": {
    "pillars": { ... },
    "elementBalance": { ... },
    "daeun": { ... },
    "ko": { "gyeokguk": "傷官格" }
  }
}
```

교체:
```json
{
  "input": { ... },
  "expected": {
    "pillars": { ... },
    "elementBalance": { ... },
    "daeun": { ... },
    "ko": {
      "gyeokguk": "傷官格",
      "yongshin": {
        "primary": "wood",
        "gisin": ["water", "metal"],
        "basisShenStrength": "종아",
        "jonggyeokKind": "가종"
      }
    },
    "cnZiping": {
      "yongshin": {
        "primary": "wood",
        "gisin": ["water", "metal"],
        "basisShenStrength": "종아",
        "structureHint": "기타"
      }
    }
  }
}
```

(`input` 영역은 그대로 유지)

- [ ] **Step 2: canonical.test.ts 의 검증 부분 확장**

`packages/saju/tests/canonical.test.ts` 의 기존 검증에 추가:

```typescript
import { buildYongshinKo } from "../src/adapters/ko/yongshin";
import { buildYongshinCnZiping } from "../src/adapters/cn-ziping/yongshin";

// 기존 검증 후 추가
if (expected.ko?.yongshin) {
  const koYong = buildYongshinKo(actualChart);
  expect(koYong.primary).toBe(expected.ko.yongshin.primary);
  expect(koYong.gisin).toEqual(expect.arrayContaining(expected.ko.yongshin.gisin));
  expect(koYong.basisShenStrength).toBe(expected.ko.yongshin.basisShenStrength);
}
if (expected.cnZiping?.yongshin) {
  const zipYong = buildYongshinCnZiping(actualChart);
  expect(zipYong.primary).toBe(expected.cnZiping.yongshin.primary);
  expect(zipYong.gisin).toEqual(expect.arrayContaining(expected.cnZiping.yongshin.gisin));
  expect(zipYong.basisShenStrength).toBe(expected.cnZiping.yongshin.basisShenStrength);
  expect(zipYong.structureHint).toBe(expected.cnZiping.yongshin.structureHint);
}
```

- [ ] **Step 3: 테스트 실행**

Run: `cd packages/saju && TEST_DATABASE_URL="postgres://test:test@127.0.0.1:5999/test_dummy" pnpm test --run tests/canonical.test.ts`
Expected: PASS. 김석곤 가종 종아 결과가 fixture 와 일치.

- [ ] **Step 4: 커밋**

```bash
git add packages/saju/tests/fixtures/canonical-1967.json packages/saju/tests/canonical.test.ts
git commit -m "test(saju-tri): canonical fixture 에 v0.3 yongshin 검증

김석곤 가종 종아 + cn-ziping 동일 결과 골든.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

### Task 6.2: 신규 fixture 3개

**Files:**
- Create: `packages/saju/tests/fixtures/fixture-jonggyeok-complete-jongjae.json`
- Create: `packages/saju/tests/fixtures/fixture-gyeokguk-shenkang-sangwan.json`
- Create: `packages/saju/tests/fixtures/fixture-gyeokguk-shenyak-jeonggwan.json`

- [ ] **Step 1: 완전 종재 fixture**

`fixture-jonggyeok-complete-jongjae.json`:
```json
{
  "input": {
    "birthDateLocal": "1980-04-08",
    "birthTimeLocal": "07:30",
    "timezone": "Asia/Seoul",
    "longitudeDeg": 126.98,
    "calendar": "solar",
    "gender": "male"
  },
  "synthetic": true,
  "expected": {
    "pillars": {
      "year":  { "stem": "戊", "branch": "辰" },
      "month": { "stem": "戊", "branch": "辰" },
      "day":   { "stem": "甲", "branch": "辰" },
      "hour":  { "stem": "戊", "branch": "辰" }
    },
    "ko": {
      "yongshin": {
        "primary": "earth",
        "gisin": ["wood", "water"],
        "basisShenStrength": "종재",
        "jonggyeokKind": "완전종"
      }
    }
  }
}
```

- [ ] **Step 2: 신강 상관격 fixture**

`fixture-gyeokguk-shenkang-sangwan.json`:
```json
{
  "input": {
    "birthDateLocal": "1990-12-15",
    "birthTimeLocal": "11:00",
    "timezone": "Asia/Seoul",
    "longitudeDeg": 126.98,
    "calendar": "solar",
    "gender": "male"
  },
  "synthetic": true,
  "expected": {
    "pillars": {
      "year":  { "stem": "庚", "branch": "午" },
      "month": { "stem": "戊", "branch": "子" },
      "day":   { "stem": "甲", "branch": "子" },
      "hour":  { "stem": "庚", "branch": "午" }
    },
    "ko": {
      "gyeokguk": "傷官格",
      "yongshin": {
        "basisShenStrength": "신강"
      }
    }
  }
}
```

- [ ] **Step 3: 신약 정관격 fixture**

`fixture-gyeokguk-shenyak-jeonggwan.json`:
```json
{
  "input": {
    "birthDateLocal": "1985-08-22",
    "birthTimeLocal": "14:30",
    "timezone": "Asia/Seoul",
    "longitudeDeg": 126.98,
    "calendar": "solar",
    "gender": "female"
  },
  "synthetic": true,
  "expected": {
    "pillars": {
      "year":  { "stem": "乙", "branch": "丑" },
      "month": { "stem": "甲", "branch": "申" },
      "day":   { "stem": "庚", "branch": "辰" },
      "hour":  { "stem": "癸", "branch": "未" }
    },
    "ko": {
      "gyeokguk": "正官格",
      "yongshin": {
        "basisShenStrength": "신약"
      }
    }
  }
}
```

- [ ] **Step 4: canonical.test.ts 의 fixture loop 갱신**

`packages/saju/tests/canonical.test.ts` 의 헤더 부분:

```typescript
import canonical1967 from "./fixtures/canonical-1967.json";
import jonggyeokJongjae from "./fixtures/fixture-jonggyeok-complete-jongjae.json";
import gyeokgukSangwan from "./fixtures/fixture-gyeokguk-shenkang-sangwan.json";
import gyeokgukJeonggwan from "./fixtures/fixture-gyeokguk-shenyak-jeonggwan.json";

const FIXTURES = [
  { name: "1967-03-29 김석곤 (가종 종아)", data: canonical1967 },
  { name: "synthetic 완전 종재", data: jonggyeokJongjae },
  { name: "synthetic 신강 상관격", data: gyeokgukSangwan },
  { name: "synthetic 신약 정관격", data: gyeokgukJeonggwan },
];
```

기존 describe 를 `describe.each(FIXTURES)` 로 변환:

```typescript
describe.each(FIXTURES)("canonical fixture — $name", ({ data }) => {
  it("expected 와 일치", () => {
    const chart = (data as any).synthetic
      ? buildChartFromExpected(data.expected.pillars)
      : computeSajuChart(data.input);

    // ko gyeokguk + yongshin 검증
    if ((data.expected as any).ko) {
      const expKo = (data.expected as any).ko;
      if (expKo.gyeokguk) {
        expect(chart.pattern).toBe(expKo.gyeokguk);
      }
      if (expKo.yongshin) {
        const koYong = buildYongshinKo(chart);
        if (expKo.yongshin.primary) {
          expect(koYong.primary).toBe(expKo.yongshin.primary);
        }
        if (expKo.yongshin.basisShenStrength) {
          expect(koYong.basisShenStrength).toBe(expKo.yongshin.basisShenStrength);
        }
        // gisin / jonggyeokKind 등은 fixture 에 있으면 검증
      }
    }
    // cnZiping 동일 패턴
  });
});

function buildChartFromExpected(pillars: any): SajuChart {
  return {
    pillars,
    elements: { wood:0, fire:0, earth:0, metal:0, water:0 },
    strength: "신약",
    tenGods: {} as any,
    pattern: pillars.pattern ?? "",
    yongSin: [], giSin: [],
    majorFortunes: [],
    inputHash: "synthetic",
  };
}
```

- [ ] **Step 5: 테스트 실행**

Run: `cd packages/saju && TEST_DATABASE_URL="postgres://test:test@127.0.0.1:5999/test_dummy" pnpm test --run tests/canonical.test.ts`
Expected: 4 fixture 모두 PASS.

합성 fixture 의 expected 가 실제 알고리즘 결과와 다르면, 한 번 결과를 찍어보고 fixture 갱신:
```typescript
// 디버깅용
console.log("ko yongshin:", JSON.stringify(buildYongshinKo(chart), null, 2));
```

→ 실제 결과로 fixture expected 정정.

- [ ] **Step 6: 커밋**

```bash
git add packages/saju/tests/fixtures/fixture-*.json packages/saju/tests/canonical.test.ts
git commit -m "test(saju-tri): 신규 fixture 3건 — 완전 종재 + 격국 분기 2종

종아·종재·종살 + 격국 4종 회귀 골든.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Phase 7: 통합 + 운영 배포

### Task 7.1: ALGORITHM_VERSION 캐시 키 사용

**Files:**
- Modify: `apps/dashboard/src/features/saju-lifetime-tri/api/narrative-server.ts`
- Modify: `apps/dashboard/src/features/saju-yearly-tri/api/narrative-server.ts`
- Modify: `apps/dashboard/src/features/saju-lifetime-tri/api/lifetime-server.ts`
- Modify: `apps/dashboard/src/features/saju-yearly-tri/api/yearly-server.ts`

- [ ] **Step 1: lifetime narrative-server.ts 의 cache where 절 갱신**

import 추가:
```typescript
import { ALGORITHM_VERSION } from "@gons/saju/lib/algorithm-version";
```

기존 where (v0.2) 의 마지막 줄에 추가:
```typescript
where: and(
  eq(sajuLifetimeNarrative.profileId, profileId),
  eq(sajuLifetimeNarrative.school, school),
  eq(sajuLifetimeNarrative.frameHash, frameHash),
  eq(sajuLifetimeNarrative.modelId, MODEL_ID),
  eq(sajuLifetimeNarrative.promptVersion, PROMPT_VERSION),
  eq(sajuLifetimeNarrative.algorithmVersion, ALGORITHM_VERSION),  // 신규
),
```

insert values 에 추가:
```typescript
.values({
  // ... 기존 필드
  promptVersion: PROMPT_VERSION,
  algorithmVersion: ALGORITHM_VERSION,  // 신규
  // ...
})
```

NarrativeResult interface 에 추가:
```typescript
export interface NarrativeResult {
  // ... v0.2 fields
  algorithmVersion: number;  // 신규
}
```

반환값 객체 두 곳 (cache hit, miss) 에 `algorithmVersion: cached.algorithmVersion` 또는 `algorithmVersion: ALGORITHM_VERSION` 추가.

- [ ] **Step 2: yearly narrative-server.ts 동일 패턴**

같은 갱신을 `apps/dashboard/src/features/saju-yearly-tri/api/narrative-server.ts` 에도 적용.

- [ ] **Step 3: lifetime-server.ts 의 sajuCharts 캐시**

`apps/dashboard/src/features/saju-lifetime-tri/api/lifetime-server.ts`:

```typescript
import { ALGORITHM_VERSION } from "@gons/saju/lib/algorithm-version";

// saju_charts 캐시 조회
const cached = await db.query.sajuCharts.findFirst({
  where: and(
    eq(sajuCharts.profileId, profileId),
    eq(sajuCharts.inputHash, hash),
    eq(sajuCharts.algorithmVersion, ALGORITHM_VERSION),  // 신규
  ),
});

// insert 시
.values({
  // ... 기존
  algorithmVersion: ALGORITHM_VERSION,
})
```

- [ ] **Step 4: yearly-server.ts 의 sajuYearlyTri 캐시**

`apps/dashboard/src/features/saju-yearly-tri/api/yearly-server.ts`:

```typescript
where: and(
  eq(sajuYearlyTri.profileId, profileId),
  eq(sajuYearlyTri.school, school),
  eq(sajuYearlyTri.targetYear, targetYear),
  eq(sajuYearlyTri.inputHash, hash),
  eq(sajuYearlyTri.schemaVersion, SCHEMA_VERSION),
  eq(sajuYearlyTri.algorithmVersion, ALGORITHM_VERSION),  // 신규
),
// insert 시도 algorithmVersion: ALGORITHM_VERSION
```

- [ ] **Step 5: 타입 체크 + 린트**

```bash
cd apps/dashboard && pnpm typecheck && pnpm lint
```
Expected: 0 errors, 0 issues.

- [ ] **Step 6: 커밋**

```bash
git add apps/dashboard/src/features/saju-lifetime-tri/api/*.ts apps/dashboard/src/features/saju-yearly-tri/api/*.ts
git commit -m "feat(saju-tri): ALGORITHM_VERSION 캐시 키 — 4 server 모듈

lifetime/yearly server + 양 narrative-server 모두 algorithmVersion 필터.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

### Task 7.2: 전체 빌드 검증

- [ ] **Step 1: 전체 typecheck + lint**

```bash
cd packages/saju && pnpm typecheck
cd ../../apps/dashboard && pnpm typecheck && pnpm lint
```
Expected: 0 errors, 0 issues.

- [ ] **Step 2: 전체 saju 패키지 테스트**

```bash
cd packages/saju && TEST_DATABASE_URL="postgres://test:test@127.0.0.1:5999/test_dummy" pnpm test --run
```
Expected: shen-strength 10 + jonggyeok 5 + gyeokguk-yongshin 9 + ko yongshin ~6 + cn-ziping yongshin ~4 + canonical 4 fixture + elements/pattern 기존 회귀 등 약 50건 모두 PASS.

- [ ] **Step 3: 전체 dashboard 테스트**

```bash
cd apps/dashboard && TEST_DATABASE_URL="postgres://test:test@127.0.0.1:5999/test_dummy" pnpm test --run features/saju-lifetime-tri features/saju-yearly-tri
```
Expected: 모든 PASS.

- [ ] **Step 4: production build**

```bash
cd /home/gon/projects/gon/gons-dashboard/.worktrees/tiger-playmcp-area
pnpm build
```
Expected: 성공.

- [ ] **Step 5: .next chunk 에 새 식별자 grep**

```bash
grep -l "shen-strength\|jonggyeok\|algorithm_version\|종아\|종재\|종살" apps/dashboard/.next/server/chunks/*.js 2>/dev/null | head -5
```
Expected: 1개 이상 chunk.

### Task 7.3: 운영 배포

- [ ] **Step 1: PR 생성 + 머지**

```bash
gh pr create --title "feat(saju-tri): v0.3 algorithm — 종격 + 격국-용신 + strength 통합" --body "$(cat <<'EOF'
spec: docs/superpowers/specs/2026-05-19-saju-v0.3-jonggyeok-algorithm-design.md
finding: docs/superpowers/findings/2026-05-19-shinhan-comparison-jonggyeok-gap.md

## Summary
- 종격(從格) 판정 룰 도입 (완전종 + 가종, 지장간 트리거)
- 격국-용신 분기 (傷官·正官·財·偏印 × 신강/신약 = 8 분기)
- lifetime/yearly strength 단일 알고리즘 통합 (자평 룰)
- ALGORITHM_VERSION=2 캐시 키로 자동 무효화 (4 테이블)

## 김석곤 검증
가종 종아격 트리거 → primary=木 / gisin=[水,金]
2026 丙午(火=재성) → 종아격에서 식상→재성 흐름은 길 → favorable
신한라이프 (대단히 좋다) 평가와 일치

## v0.4 backlog
B1 가중치 점수 / C1 grade 5분 / mangpai·jp 종격 / 잔여 격국 6종

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"

gh run watch
gh pr merge --squash
```

- [ ] **Step 2: 운영 DB migration**

```bash
cd /home/gon/projects/gon/gons-dashboard  # main repo (worktree 아님)
git pull origin main
I_KNOW_THIS_IS_PROD=1 pnpm db:migrate
```

- [ ] **Step 3: 컬럼 갱신 검증**

```bash
for table in saju_charts saju_yearly_tri saju_lifetime_narrative saju_yearly_narrative; do
  echo "--- $table ---"
  docker --context home-server exec gons-dashboard-postgres psql -U gons -d gons_dashboard \
    -c "\d $table" | grep algorithm_version
done
```
Expected: 4 테이블 모두 출력.

- [ ] **Step 4: 도커 이미지 교체**

```bash
COMPOSE=/home/gon/projects/gon/gons-dashboard/docker-compose.yml
docker --context home-server compose -f $COMPOSE pull app cron
docker --context home-server compose -f $COMPOSE up -d app cron
```

- [ ] **Step 5: 헬스체크**

```bash
ssh gon@192.168.0.5 "curl -s http://localhost:3020/api/health"
```
Expected: `{"status":"ok",...}`

- [ ] **Step 6: 사용자 브라우저 검증**

`https://gons.krdn.kr/fortune/76896374-e6a5-47e3-8a0b-0347ac2944d2` 에 로그인 후:
- yearly 탭 2026 → 4학파 평가 확인. ko / cn-ziping 가 *favorable* 또는 *mixed* 로 바뀌어야 함
- "더 자세히 보기" 클릭 → cache miss → ~30초 LLM → 새 narrative

- [ ] **Step 7: v2 row 생성 확인**

```bash
docker --context home-server exec gons-dashboard-postgres psql -U gons -d gons_dashboard -c \
  "SELECT school, target_year, algorithm_version FROM saju_yearly_tri WHERE algorithm_version = 2 ORDER BY generated_at DESC LIMIT 8;"
```
Expected: 김석곤 2026 의 4학파 행 (algorithm_version=2) 출력.

- [ ] **Step 8: 종격 hit rate 확인**

```bash
docker --context home-server exec gons-dashboard-postgres psql -U gons -d gons_dashboard -c \
  "SELECT strength, COUNT(*) FROM saju_charts WHERE algorithm_version = 2 GROUP BY strength;"
```
Expected: 종아·종재·종살 합계 < 10% (정통 엄격도).

---

## 참고 사항

### 운영 모니터링 (배포 후 1주)

- 김석곤 2026 narrative 가 favorable 로 바뀌었는지
- 다른 프로파일이 예상치 못하게 뒤집히지 않았는지
- LLM 호출 빈도·비용 증가 추세
- 종격 트리거 hit rate < 10% 유지

### 롤백 절차

빠른 롤백:
1. `packages/saju/src/lib/algorithm-version.ts` 의 `ALGORITHM_VERSION = 2` → `1`
2. 도커 재배포
3. v1 row 자동 hit (DB schema 호환)

### Spec 대비 의도적 단순화

- mangpai/jp 학파 종격 미반영 (v0.4)
- B1 가중치, C1 5분 grade 미반영 (v0.4)
- C2 fixture 9건 광범위 회귀 — 이번엔 4건만
- lifetime narrative prompt 의 종격 설명 — LLM 자율, PROMPT_VERSION bump 안 함

### 의존성 그래프

```
algorithm-version.ts (상수)
branchHiddenStems.ts (지장간 표)
   ↑
shen-strength.ts (자평 룰 + 종격 판정)
   ↑              ↑
jonggyeok.ts    gyeokguk-yongshin.ts
   ↑              ↑
adapters/ko/yongshin.ts
adapters/cn-ziping/yongshin.ts
   ↑
adapters/{ko,cn-ziping}/lifetime.ts (기존, 영향 받음)
adapters/{ko,cn-ziping}/yearly.ts (기존, 영향 받음)
   ↑
apps/dashboard/src/features/saju-{lifetime,yearly}-tri/api/*.ts
   ↑ (ALGORITHM_VERSION 캐시 키)
DB (saju_charts, yearly_tri, lifetime_narrative, yearly_narrative)
```

### v0.4 backlog

- B1 강약 가중치 (월령·일지·천간·지지·통근 차등 점수)
- C1 grade 5분 척도
- C2 fixture 광범위 회귀 (종격 후보 9건)
- mangpai/jp 학파의 종격 룰 적용
- 잔여 격국 6종 (식신격·정재격·편재격·편관격·정인격·건록격·양인격)
- 강약 점수 신한라이프 호환 표현 (23:127 식)
